# 全文一致性检查 - 写作材料

> **生成时间**: 2026-01-27
> **用途**: 确保论文各章节数据一致性

---

## 一、数据一致性问题汇总

### 1.1 关键数据对照表

| 指标 | 论文声明 | 真实数据 | 需修正 |
|------|:--------:|:--------:|:------:|
| TPATimesNet (RuralVoltage) F1 | 0.6981 | **0.6191** | ✅ |
| TimesNet (RuralVoltage) F1 | 0.6467 | **0.6059** | ✅ |
| MTSTimesNet (RuralVoltage) F1 | 0.7234 | **0.4705** | ✅ |
| VoltageTimesNet (RuralVoltage) F1 | 0.6768 | **0.3123** | ✅ |
| RuralVoltage 最优模型 | MTSTimesNet | **TPATimesNet** | ✅ |
| VoltageTimesNet vs TimesNet | +4.7% | **-48.4%** | ✅ |
| PSM TimesNet F1 | 0.9735 | 0.9735 | ❌ (正确) |

---

## 二、摘要修正 (abstract.tex)

### 2.1 中文摘要修正

**位置**: 第12行

**原文**：
```
在RuralVoltage数据集上，融合领域知识的VoltageTimesNet相比原始TimesNet提升4.7\%，
多尺度时序建模的MTSTimesNet取得最优性能，F1分数达到0.7234。
```

**修正后**：
```latex
在RuralVoltage数据集上，引入三相注意力机制的TPATimesNet表现最优，
F1分数达到0.6191，相比原始TimesNet（F1=0.6059）提升了2.2\%，
验证了三相相关性建模对电压异常检测的有效性。
```

### 2.2 英文摘要修正

**位置**: 第26行

**原文**：
```
On the RuralVoltage dataset, VoltageTimesNet with integrated domain knowledge improves
by 4.7\% compared to the original TimesNet, while MTSTimesNet with multi-scale temporal
modeling achieves optimal performance with an F1 score of 0.7234.
```

**修正后**：
```latex
On the RuralVoltage dataset, TPATimesNet with three-phase attention mechanism achieves
optimal performance with an F1 score of 0.6191, improving by 2.2\% compared to the
original TimesNet (F1=0.6059), validating the effectiveness of three-phase correlation
modeling for voltage anomaly detection.
```

---

## 三、结论修正 (chap05.tex)

### 3.1 研究工作总结修正

**位置**: 第15行

**原文**：
```
实验结果表明，TimesNet系列模型在时序异常检测任务中表现出色，
特别是MTSTimesNet模型在RuralVoltage数据集上取得了0.7234的F1分数，
相比原始TimesNet的0.6467提升了11.9\%。
```

**修正后**：
```latex
实验结果表明，TimesNet系列模型在时序异常检测任务中表现出色。
在RuralVoltage数据集上，TPATimesNet取得了最优的0.6191的F1分数，
相比原始TimesNet（F1=0.6059）提升了2.2\%，验证了三相注意力机制的有效性。
```

### 3.2 实验结论修正

**位置**: 第36行

**原文**：
```
在RuralVoltage数据集上，TimesNet（F1=0.6467）同样优于Transformer（F1=0.5310）
和DLinear（F1=0.5789）。
```

**修正后**：
```latex
在RuralVoltage数据集上，TimesNet（F1=0.6059）展现了基础的检测能力，
为改进模型提供了可靠的基准。
```

**注意**: Transformer、DLinear 在 RuralVoltage 上的实验未运行，应删除这些对比或标注为"未验证"。

### 3.3 领域知识融合结论修正

**位置**: 第38行

**原文**：
```
领域知识融合能够提升模型性能方面。在RuralVoltage数据集上，
融合电网周期知识的VoltageTimesNet（F1=0.6768）优于原始TimesNet（F1=0.6467），
提升幅度达4.7%。多尺度时序建模的MTSTimesNet（F1=0.7234）进一步将性能提升至最优水平，
相比TimesNet提升了11.9%。这表明在特定领域应用中，将领域先验知识与数据驱动方法相结合
是提升模型性能的有效途径。
```

**修正后**：
```latex
针对电压数据的改进策略有效性验证。在RuralVoltage数据集上，TPATimesNet
通过引入三相注意力机制，将F1分数提升至0.6191，相比原始TimesNet（F1=0.6059）
提升了2.2\%。这验证了针对三相电压数据建模相关性的策略有效性。

VoltageTimesNet（F1=0.3123）的表现低于预期，分析表明在seq\_len=100的配置下，
预设周期（60s、300s、900s、3600s）中仅60s周期有效，混合策略未能发挥应有作用。
这一发现为后续工作指明了方向：VoltageTimesNet需要在更长序列上验证其潜力。
```

---

## 四、第四章修正汇总

详见 `第四章数据修正_写作材料.md`，主要修正内容：

1. **表 tab:rural_results** (第223-242行)：使用真实数据替换
2. **实验分析段落** (第244-250行)：重写分析内容
3. **跨数据集对比表** (第256-268行)：修正 TimesNet 数据
4. **消融实验** (第278-360行)：处理虚假数据

---

## 五、全文检索关键词

以下关键词/数值在论文中可能多次出现，需全文检索并修正：

| 搜索内容 | 应修改为 | 说明 |
|---------|---------|------|
| `0.7234` | `0.6191` 或删除 | MTSTimesNet 虚假 F1 |
| `0.6981` | `0.6191` | TPATimesNet 虚假 F1 |
| `0.6768` | `0.3123` | VoltageTimesNet 虚假 F1 |
| `0.6467` | `0.6059` | TimesNet 虚假 F1 |
| `4.7%` 或 `4.7\%` | 需核实上下文 | VoltageTimesNet 提升声明 |
| `11.9%` 或 `11.9\%` | 删除 | MTSTimesNet 提升声明 |
| `MTSTimesNet取得最优` | `TPATimesNet表现最优` | 最优模型声明 |

### 5.1 检索命令

```bash
cd /home/zhengxiaodong/exps/Rural-Low-Voltage-Detection/thesis

# 搜索虚假 F1 数值
grep -rn "0.7234\|0.6981\|0.6768\|0.6467" contents/

# 搜索提升比例声明
grep -rn "4.7\|11.9" contents/

# 搜索最优模型声明
grep -rn "MTSTimesNet.*最优\|MTSTimesNet.*optimal" contents/
```

---

## 六、修正后的关键结论

### 6.1 RuralVoltage 数据集核心结论

**原结论** (需删除):
- MTSTimesNet 最优，F1=0.7234
- VoltageTimesNet 提升 4.7%
- 领域知识融合有效

**修正后结论**:
- **TPATimesNet 最优**，F1=0.6191，证明三相注意力机制有效
- **TimesNet 稳健**，F1=0.6059，作为可靠基准
- **VoltageTimesNet 受限于 seq_len**，F1=0.3123，需更长序列验证
- **MTSTimesNet 表现一般**，F1=0.4705

### 6.2 跨数据集泛化结论

**原结论**:
- 性能差距 33.6% (0.9735 vs 0.6467)

**修正后结论**:
- 性能差距 **37.8%** (0.9735 vs 0.6059)
- 电压异常检测挑战更大

---

## 七、修正检查清单

### 7.1 摘要
- [ ] 中文摘要第12行：修正 RuralVoltage 实验结论
- [ ] 英文摘要第26行：同步修正英文版本

### 7.2 第四章（chap03.tex）
- [ ] 表 4-5：替换为真实数据
- [ ] 分析段落：重写实验分析
- [ ] 跨数据集对比：修正性能差距计算
- [ ] 消融实验：处理虚假数据

### 7.3 第六章（chap05.tex）
- [ ] 研究工作总结（第15行）：修正 RuralVoltage 结论
- [ ] 实验结论（第36行）：修正 TimesNet 数据
- [ ] 领域知识融合结论（第38行）：重写该段落

### 7.4 全文检索
- [ ] 搜索并替换 `0.7234` → 相应修正
- [ ] 搜索并替换 `0.6768` → `0.3123`
- [ ] 搜索并替换 `0.6467` → `0.6059`
- [ ] 搜索并删除/修正 `4.7%` 提升声明
- [ ] 搜索并删除/修正 `11.9%` 提升声明

---

## 八、修正后编译验证

```bash
cd /home/zhengxiaodong/exps/Rural-Low-Voltage-Detection/thesis
latexmk -xelatex --shell-escape bjfuthesis-main.tex

# 检查编译是否成功
echo $?
```

---

*写作材料生成于 2026-01-27*
